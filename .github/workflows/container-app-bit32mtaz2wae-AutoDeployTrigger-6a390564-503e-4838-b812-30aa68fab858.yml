name: Trigger auto deployment for container-app-bit32mtaz2wae

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - 'server/**'
    - '.github/workflows/container-app-bit32mtaz2wae-AutoDeployTrigger-6a390564-503e-4838-b812-30aa68fab858.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPBIT32MTAZ2WAE_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPBIT32MTAZ2WAE_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPBIT32MTAZ2WAE_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/server 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPBIT32MTAZ2WAE_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPBIT32MTAZ2WAE_REGISTRY_PASSWORD }}
          containerAppName: container-app-bit32mtaz2wae
          resourceGroup: todonodejspgsqlgoogleauth89329578
          imageToBuild: default/container-app-bit32mtaz2wae:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


